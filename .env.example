# ToxiGuard Environment Variables
# ── Using Railway MySQL (https://railway.app) ──

PORT=3000

# Railway MySQL credentials
# Get these from: Railway Dashboard → Your Project → MySQL service → Variables tab
DB_HOST=your-railway-host.railway.app
DB_PORT=your_railway_port        # Usually a 5-digit port like 12345 (NOT 3306)
DB_USER=root
DB_PASS=your_railway_password
DB_NAME=railway

# JWT Secret - any long random string
JWT_SECRET=toxiguard_super_secret_jwt_key_2024_change_this
